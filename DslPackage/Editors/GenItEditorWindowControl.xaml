<UserControl x:Class="Dyvenix.GenIt.DslPackage.Editors.GenItEditorWindowControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vsshell="clr-namespace:Microsoft.VisualStudio.Shell;assembly=Microsoft.VisualStudio.Shell.15.0"
             mc:Ignorable="d" 
             d:DesignHeight="500" d:DesignWidth="950"
             Background="{DynamicResource {x:Static vsshell:VsBrushes.ToolWindowBackgroundKey}}">
    <UserControl.Resources>
        <!-- VS Theme-aware resources -->
        <Style x:Key="NoSelectionTextStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource {x:Static vsshell:VsBrushes.GrayTextKey}}"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
    </UserControl.Resources>
    
    <Grid>
        <!-- Empty state message - shown when no supported element is selected -->
        <TextBlock x:Name="txtNoSelection" 
                   Text="No item selected" 
                   Style="{StaticResource NoSelectionTextStyle}"
                   Visibility="Visible"/>
        
        <!-- Content host - dynamically shows the appropriate editor control -->
        <ContentPresenter x:Name="editorContentHost" Visibility="Collapsed"/>
    </Grid>
</UserControl>
