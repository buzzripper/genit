<editors:UserControlBase x:Class="Dyvenix.GenIt.DslPackage.Editors.Entity.Controls.EntityEditControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Dyvenix.GenIt.DslPackage.Editors.Entity.Controls"
             xmlns:editors="clr-namespace:Dyvenix.GenIt.DslPackage.Editors"
             xmlns:controls="clr-namespace:Dyvenix.GenIt.DslPackage.Editors.Controls"
             xmlns:vsshell="clr-namespace:Microsoft.VisualStudio.Shell;assembly=Microsoft.VisualStudio.Shell.15.0"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="900"
             Background="{DynamicResource {x:Static vsshell:VsBrushes.ToolWindowBackgroundKey}}">
    <UserControl.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource {x:Static vsshell:VsBrushes.ToolWindowTextKey}}"/>
        </Style>
        <Style TargetType="Label">
            <Setter Property="Foreground" Value="{DynamicResource {x:Static vsshell:VsBrushes.ToolWindowTextKey}}"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="{DynamicResource {x:Static vsshell:VsBrushes.ToolWindowBackgroundKey}}"/>
            <Setter Property="Foreground" Value="{DynamicResource {x:Static vsshell:VsBrushes.ToolWindowTextKey}}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource {x:Static vsshell:VsBrushes.ToolWindowBorderKey}}"/>
            <Setter Property="Padding" Value="4,2"/>
        </Style>
        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="{DynamicResource {x:Static vsshell:VsBrushes.ToolWindowTextKey}}"/>
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="{DynamicResource {x:Static vsshell:VsBrushes.ToolWindowBackgroundKey}}"/>
            <Setter Property="Foreground" Value="{DynamicResource {x:Static vsshell:VsBrushes.ToolWindowTextKey}}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource {x:Static vsshell:VsBrushes.ToolWindowBorderKey}}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton x:Name="ToggleButton" 
                                          Background="{DynamicResource {x:Static vsshell:VsBrushes.ToolWindowBackgroundKey}}"
                                          BorderBrush="{DynamicResource {x:Static vsshell:VsBrushes.ToolWindowBorderKey}}"
                                          BorderThickness="1"
                                          Focusable="False"
                                          IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                          ClickMode="Press">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition/>
                                                <ColumnDefinition Width="20"/>
                                            </Grid.ColumnDefinitions>
                                            <Border Grid.ColumnSpan="2" 
                                                    Background="{DynamicResource {x:Static vsshell:VsBrushes.ToolWindowBackgroundKey}}"
                                                    BorderBrush="{DynamicResource {x:Static vsshell:VsBrushes.ToolWindowBorderKey}}"
                                                    BorderThickness="1"/>
                                            <Border Grid.Column="1" 
                                                    Background="{DynamicResource {x:Static vsshell:VsBrushes.ToolWindowBackgroundKey}}"
                                                    BorderBrush="{DynamicResource {x:Static vsshell:VsBrushes.ToolWindowBorderKey}}"
                                                    BorderThickness="1,0,0,0"/>
                                            <Path Grid.Column="1" Data="M 0 0 L 4 4 L 8 0 Z" 
                                                  Fill="{DynamicResource {x:Static vsshell:VsBrushes.ToolWindowTextKey}}"
                                                  HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Grid>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>
                            <ContentPresenter x:Name="ContentSite"
                                              Content="{TemplateBinding SelectionBoxItem}"
                                              ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                              ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                              Margin="6,3,23,3"
                                              IsHitTestVisible="False"
                                              HorizontalAlignment="Left"
                                              VerticalAlignment="Center"/>
                            <Popup x:Name="Popup" Placement="Bottom"
                                   IsOpen="{TemplateBinding IsDropDownOpen}"
                                   AllowsTransparency="True" Focusable="False"
                                   PopupAnimation="Slide">
                                <Grid x:Name="DropDown" MinWidth="{TemplateBinding ActualWidth}"
                                      MaxHeight="{TemplateBinding MaxDropDownHeight}" SnapsToDevicePixels="True">
                                    <Border x:Name="DropDownBorder"
                                            Background="{DynamicResource {x:Static vsshell:VsBrushes.ToolWindowBackgroundKey}}"
                                            BorderBrush="{DynamicResource {x:Static vsshell:VsBrushes.ToolWindowBorderKey}}"
                                            BorderThickness="1"/>
                                    <ScrollViewer Margin="4,6,4,6" SnapsToDevicePixels="True">
                                        <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained"/>
                                    </ScrollViewer>
                                </Grid>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="ComboBoxItem">
            <Setter Property="Background" Value="{DynamicResource {x:Static vsshell:VsBrushes.ToolWindowBackgroundKey}}"/>
            <Setter Property="Foreground" Value="{DynamicResource {x:Static vsshell:VsBrushes.ToolWindowTextKey}}"/>
            <Setter Property="Padding" Value="4,2"/>
            <Style.Triggers>
                <Trigger Property="IsHighlighted" Value="True">
                    <Setter Property="Background" Value="{DynamicResource {x:Static vsshell:VsBrushes.ToolWindowTabMouseOverBackgroundBeginKey}}"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>
    
    <Grid Margin="16">
        <Grid.ColumnDefinitions>
            <ColumnDefinition x:Name="leftColumn" Width="*" MinWidth="250"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition x:Name="rightColumn" Width="*" MinWidth="250"/>
        </Grid.ColumnDefinitions>

        <!-- Left Panel - Basic Properties -->
        <Grid Grid.Column="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Entity Header -->
            <TextBlock Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" Text="Entity" 
                       FontSize="14" FontWeight="SemiBold" Margin="0,0,0,12"/>

            <!-- Name -->
            <Label Grid.Row="1" Grid.Column="0" Content="Name:" VerticalAlignment="Center" Margin="0,0,8,8"/>
            <TextBox Grid.Row="1" Grid.Column="1" x:Name="txtName" VerticalAlignment="Center" Margin="0,0,0,8"
                     TextChanged="txtName_TextChanged"/>

            <!-- Description -->
            <Label Grid.Row="2" Grid.Column="0" Content="Description:" VerticalAlignment="Center" Margin="0,0,8,8"/>
            <TextBox Grid.Row="2" Grid.Column="1" x:Name="txtDescription" VerticalAlignment="Center" Margin="0,0,0,8"
                     TextChanged="txtDescription_TextChanged"/>

            <!-- Module -->
            <Label Grid.Row="3" Grid.Column="0" Content="Module:" VerticalAlignment="Center" Margin="0,0,8,8"/>
            <ComboBox Grid.Row="3" Grid.Column="1" x:Name="cboModule" VerticalAlignment="Center" Margin="0,0,0,8"
                      SelectionChanged="cboModule_SelectionChanged"/>

            <!-- Generate Code -->
            <Label Grid.Row="4" Grid.Column="0" Content="Generate Code:" VerticalAlignment="Center" Margin="0,0,8,8"/>
            <CheckBox Grid.Row="4" Grid.Column="1" x:Name="chkGenerateCode" VerticalAlignment="Center" Margin="0,0,0,8"
                      Checked="chkGenerateCode_Changed" Unchecked="chkGenerateCode_Changed"/>

            <!-- Auditable -->
            <Label Grid.Row="5" Grid.Column="0" Content="Auditable:" VerticalAlignment="Center" Margin="0,0,8,8"/>
            <CheckBox Grid.Row="5" Grid.Column="1" x:Name="chkAuditable" VerticalAlignment="Center" Margin="0,0,0,8"
                      Checked="chkAuditable_Changed" Unchecked="chkAuditable_Changed"/>

            <!-- Include Row Version -->
            <Label Grid.Row="6" Grid.Column="0" Content="Incl Row Version:" VerticalAlignment="Center" Margin="0,0,8,8"/>
            <CheckBox Grid.Row="6" Grid.Column="1" x:Name="chkInclRowVersion" VerticalAlignment="Center" Margin="0,0,0,8"
                      Checked="chkInclRowVersion_Changed" Unchecked="chkInclRowVersion_Changed"/>
        </Grid>

        <!-- GridSplitter -->
        <GridSplitter x:Name="gridSplitter" Grid.Column="1" Width="5" HorizontalAlignment="Center" VerticalAlignment="Stretch"
                      Background="{DynamicResource {x:Static vsshell:VsBrushes.ToolWindowBorderKey}}"
                      Margin="8,0" DragCompleted="gridSplitter_DragCompleted"/>

        <!-- Right Panel - Attributes and Usings -->
        <Grid Grid.Column="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Enabled checkbox at top-right -->
            <CheckBox Grid.Row="0" x:Name="chkEnabled" Content="Enabled"
                      HorizontalAlignment="Right" Margin="0,0,0,12"
                      Checked="chkEnabled_Changed" Unchecked="chkEnabled_Changed"/>

            <!-- Attributes -->
            <controls:StringListControl Grid.Row="1" x:Name="attributesControl"
                                        Title="Attributes" DialogTitle="Add Attribute" DialogLabel="Attribute:"
                                        Margin="0,0,0,8"
                                        ItemsChanged="attributesControl_ItemsChanged"/>

            <!-- Usings -->
            <controls:StringListControl Grid.Row="2" x:Name="usingsControl"
                                        Title="Usings" DialogTitle="Add Using" DialogLabel="Using:"
                                        ItemsChanged="usingsControl_ItemsChanged"/>
        </Grid>
    </Grid>
</editors:UserControlBase>
