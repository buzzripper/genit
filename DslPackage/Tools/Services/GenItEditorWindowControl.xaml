<UserControl x:Class="Dyvenix.GenIt.DslPackage.Tools.Services.GenItEditorWindowControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:controls="clr-namespace:Dyvenix.GenIt.DslPackage.Tools.Services.Controls"
             xmlns:vsshell="clr-namespace:Microsoft.VisualStudio.Shell;assembly=Microsoft.VisualStudio.Shell.15.0"
             mc:Ignorable="d" 
             d:DesignHeight="500" d:DesignWidth="950"
             Background="{DynamicResource {x:Static vsshell:VsBrushes.ToolWindowBackgroundKey}}">
    <UserControl.Resources>
        <!-- VS Theme-aware resources -->
        <Style x:Key="NoSelectionTextStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource {x:Static vsshell:VsBrushes.GrayTextKey}}"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
    </UserControl.Resources>
    
    <Grid>
        <!-- Empty state message - shown when no ServiceModel is selected -->
        <TextBlock x:Name="txtNoSelection" 
                   Text="No item selected" 
                   Style="{StaticResource NoSelectionTextStyle}"
                   Visibility="Visible"/>
        
        <!-- Service Edit Control - shown when a ServiceModel is selected -->
        <controls:SvcEditControl x:Name="svcEditControl" Visibility="Collapsed"/>
    </Grid>
</UserControl>
