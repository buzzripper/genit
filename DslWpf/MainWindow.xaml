<Window x:Class="DslWpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:controls="clr-namespace:DslWpf.Controls"
        mc:Ignorable="d"
        Title="Service Editor - WPF Version" 
        Height="600" Width="1100"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource Theme.Background.Window}"
        Loaded="Window_Loaded">
    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="{DynamicResource Theme.Background.Header}" 
                Padding="12,8" Margin="0,0,0,8"
                BorderBrush="{DynamicResource Theme.Border}" 
                BorderThickness="1">
            <StackPanel>
                <TextBlock Text="Service Editor Control (WPF)" FontSize="18" FontWeight="Bold"
                           Foreground="{DynamicResource Theme.Foreground}"/>
                <TextBlock Text="This is a WPF port of the WinForms SvcEditCtl user control" 
                           FontSize="12" Foreground="{DynamicResource Theme.Foreground.Secondary}" Margin="0,4,0,0"/>
            </StackPanel>
        </Border>

        <!-- Main Content - SvcEditControl -->
        <Border Grid.Row="1" BorderBrush="{DynamicResource Theme.Border}" 
                BorderThickness="1">
            <controls:SvcEditControl x:Name="svcEditCtl"/>
        </Border>

        <!-- Footer -->
        <Border Grid.Row="2" Background="{DynamicResource Theme.Background.Header}" 
                Padding="8" Margin="0,8,0,0"
                BorderBrush="{DynamicResource Theme.Border}" 
                BorderThickness="1">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Refresh Sample Data" Click="btnRefresh_Click" Padding="12,4" Margin="0,0,8,0"/>
                <Button Content="Show Model State" Click="btnShowState_Click" Padding="12,4"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
